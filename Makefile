.PHONY: help

help:
	@echo "Usage:"
	@echo "		make consumer								run consumer service"
	@echo "		make producer								run producer service"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make docs										run local documentation server"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"
	@echo	"		make dockerbuild						build the docker image"

